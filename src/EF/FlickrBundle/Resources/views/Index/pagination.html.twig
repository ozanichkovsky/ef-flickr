<div class="pagination">
    {% for page in range(1, pages) %}
        {% if page == current %}
            {{ page }}
        {% else %}
            <a href="{{ url('_index', {'page': page}) }}">{{ page }}</a>
        {% endif %}
        {% if not loop.last %}
            |
        {% endif %}
    {% endfor %}
</div>